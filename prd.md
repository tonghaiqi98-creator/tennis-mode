# 云台新增网球模式 PRD

## 1. 需求背景

当前用户在网球训练、对打、约球等场景中存在较强的拍摄需求，尤其是希望在无人协助的情况下记录自己的击球动作、跑动回合和可分享片段。用研结论显示，网球场景下用户核心痛点集中在「人物容易跑出画面」「设备架好后无法实时调整」「功能上手复杂」「长时间录制素材过多、后期筛选麻烦」等问题。

网球运动具有横向移动范围大、急停变向频繁、发球/挥拍动作幅度大、单次拍摄时长较长等特点。普通固定三脚架虽然可以满足基础记录，但容易出现人物出画、构图像监控、后期素材冗余等问题；通用人物追踪又容易因为人物移动快、多人干扰、画面拍太满而导致跟踪不稳。

因此，本需求新增「网球模式」，面向无人值守的网球拍摄场景，通过**场地标尺式架机位引导、默认广构图、自动人物识别、轻量跟随修正、重点回合保存**，帮助用户更稳定、更省心地记录网球过程。

---

## 2. 需求定位

网球模式不是专业网球分析工具，也不是以识别网球轨迹为核心的算法模式。

本期定位为：

> 面向单人/双人网球无人值守拍摄场景，帮助用户在无法实时查看画面的情况下，完成合理架机位、减少人物出画，并自动保留更有价值的运动片段。

核心要解决四个问题：

| 问题   | 说明                         |
| ---- | -------------------------- |
| 拍不到  | 人物横向移动大，容易跑出画面             |
| 不会拍  | 用户架机位时场上没有自己，无法以自己为参照调整构图  |
| 拍不好  | 普通追踪容易画面甩动、人物太满、构图不好看      |
| 不好处理 | 一场球时间长，保存几十分钟素材不合理，后期筛选成本高 |

---

## 3. 目标用户

| 用户类型  | 使用场景        | 核心诉求            |
| ----- | ----------- | --------------- |
| 网球爱好者 | 日常约球、双人对打   | 不求人拍摄，记录完整回合    |
| 训练用户  | 发球、正反手、多球训练 | 复盘动作、步伐和击球状态    |
| 内容创作者 | 运动短视频、社媒分享  | 拍到好看的跑动、击球、高光片段 |
| 轻度用户  | 偶尔打球记录生活    | 操作简单，不需要理解复杂参数  |

---

## 4. 需求目标

| 目标       | 说明                           |
| -------- | ---------------------------- |
| 降低架机位成本  | 用户不在场内时，也能通过场地线和球网完成取景调整     |
| 提升人物入画率  | 默认覆盖主要运动区域，减少横向移动、发球、跑动时出画   |
| 保持画面稳定   | 避免强追踪导致画面频繁甩动                |
| 降低操作依赖   | 不依赖拍摄中手动点选、手势控制或实时查看手机       |
| 提升有效素材比例 | 自动识别并优先保存长回合、多拍回合、长跑动回合等重点片段 |
| 降低存储压力   | 避免整场几十分钟素材全部以高清形式保存          |

---

## 5. 使用场景假设

### 5.1 核心场景：无人值守拍摄

用户通常的实际路径是：

```text
到达球场
↓
将手机/云台放在场边、底线后方或侧后方
↓
打开网球模式
↓
根据场地线、球网、发球区等标尺调整画面
↓
点击开始
↓
用户进入场内打球
↓
拍摄过程中无法实时查看画面，也无法手动选择主体
↓
打完后查看自动保存的重点片段
```

因此，本需求不把「拍摄中手动点选主体」作为核心路径。

---

## 6. 需求范围

### 6.1 本期包含

| 模块    | 功能            | 说明                         |
| ----- | ------------- | -------------------------- |
| 模式入口  | 新增网球模式        | 在运动模式下新增「网球」               |
| 架机位引导 | 场地标尺式取景引导     | 用户不在场内时，基于球场线、球网、底线等元素完成取景 |
| 架设建议  | 高度/方向/位置建议    | 推荐腰到胸口高度、横屏、场边/底线后方位置      |
| 默认构图  | 广构图优先         | 默认保留横向移动、挥拍、发球空间           |
| 自动识别  | 自动识别人/场地      | 不依赖拍摄中手动点选主体               |
| 云台策略  | 轻量跟随修正        | 人物接近边缘时进行修正，避免强追           |
| 防误切   | 多人场景稳定策略      | 多人入画时避免频繁切换主体              |
| 重点保存  | 自动识别重点片段      | 多拍回合、长跑动回合、明显击球段优先高清保存     |
| 素材处理  | 非重点时段降级处理     | 非重点内容不全部高清保留，降低存储压力        |
| 状态提示  | 拍摄前/拍摄中/拍摄后提示 | 提示取景、光线、存储、片段保存状态          |
| 埋点    | 关键行为上报        | 进入、开始、识别、保存、退出等行为上报        |

### 6.2 本期不包含

| 功能       | 说明                    |
| -------- | --------------------- |
| 网球轨迹精确识别 | 不以追踪网球轨迹为本期核心         |
| 比分识别     | 不识别局分、盘分、发球方          |
| 专业动作分析   | 不输出击球质量、挥拍评分、落点分析     |
| 完整战术分析   | 不分析技战术和站位策略           |
| 全自动成片    | 本期重点是保存高价值素材，不做复杂剪辑成片 |

---

# 7. 产品方案

## 7.1 模式入口

在运动类模式下新增「网球」。

建议路径：

```text
拍摄页 → 模式选择 → 运动 → 网球
```

如果当前已有「大众运动」集合，则放置在：

```text
拍摄页 → 大众运动 → 网球
```

进入网球模式后，系统自动启用：

| 能力      | 默认策略 |
| ------- | ---- |
| 取景引导    | 开启   |
| 横屏建议    | 开启   |
| 广角/广构图  | 默认开启 |
| 自动人物识别  | 开启   |
| 轻量跟随修正  | 开启   |
| 重点片段识别  | 开启   |
| 高清重点保存  | 开启   |
| 非重点降级处理 | 开启   |

---

## 7.2 拍摄前架机位引导

### 7.2.1 问题

网球模式的特殊点是：

> 用户布置机位时，场上通常没有自己，不能以自己作为画面参照。

所以不能只提示「请将人物置于画面中」。
更合理的方式是用**网球场本身作为标尺**，让用户根据球场结构调整机位。

---

### 7.2.2 引导原则

拍摄前引导应基于以下参照物：

| 参照物     | 作用               |
| ------- | ---------------- |
| 球网      | 判断画面水平关系和场地中线    |
| 底线      | 判断后场活动范围是否覆盖     |
| 单打/双打边线 | 判断横向移动范围是否覆盖     |
| 发球线/发球区 | 判断中前场是否入画        |
| 场地左右边界  | 判断球员横向跑动空间是否足够   |
| 天空/上方空间 | 判断发球、抛球、挥拍是否容易出画 |

---

### 7.2.3 推荐机位

| 项目   | 建议            |
| ---- | ------------- |
| 架设高度 | 腰部至胸口高度       |
| 拍摄方向 | 优先横屏          |
| 设备位置 | 场边、底线后方、侧后方   |
| 画面范围 | 覆盖主要半场或完整运动区域 |
| 上方留白 | 发球/高举球拍时不易出画  |
| 左右留白 | 覆盖横向跑动范围      |

推荐提示文案：

```text
建议将设备架设在腰部至胸口高度，并使用横屏拍摄。
```

```text
请让画面覆盖底线、球网和主要跑动区域，减少打球时出画。
```

---

### 7.2.4 场地标尺式取景引导

进入网球模式后，预览画面可提供简化取景辅助线。



#### 方案 B：推荐版，场地参考框

在画面中展示一个「网球场参考框」，帮助用户调整取景。

用户无需站到场内，只需要让球场结构尽量落在参考框内。

示意：

```text
┌────────────────────┐
│   上方留白区域       │
│                    │
│  ┌──────────────┐  │
│  │  主要运动区域 │  │
│  │              │  │
│  │──── 球网 ────│  │
│  │              │  │
│  └──────────────┘  │
│                    │
└────────────────────┘
```

提示文案：

```text
请将球网、底线和主要跑动区域放入参考框内。
```

```text
画面覆盖越完整，打球时越不容易出画。
```

优点：更符合无人值守架机位场景。
缺点：需要设计参考框样式，但不一定依赖算法识别场地。

---


### 7.2.5 本期建议

本期建议采用：

> 场地参考框 + 文案提示

不强依赖场地识别算法，先通过视觉辅助和简短提示降低架机位成本。

也就是：

| 能力        | 本期建议             |
| --------- | ---------------- |
| 场地参考框     | 做                |
| 球网/底线自动识别 | 可选，不作为 P0        |
| 人物站位校准    | 不作为核心，因为拍摄前场上无用户 |
| 纯人物识别提示   | 不作为主要引导          |

---

## 7.3 默认构图策略

### 7.3.1 原则

网球模式默认不采用强跟踪和动态长焦策略。

核心原则：

> 先保证拍得到，再保证拍得近；先保证稳定，再进行跟随。

---

### 7.3.2 默认画面策略

| 项目   | 策略            |
| ---- | ------------- |
| 默认视角 | 更广视角/广构图      |
| 人物大小 | 不拍太满，保留移动空间   |
| 左右空间 | 保留横向跑动空间      |
| 上方空间 | 保留发球、抛球、挥拍空间  |
| 下方空间 | 尽量保留脚步移动      |
| 场地信息 | 保留球网、场地线、运动环境 |

---

## 7.4 自动人物识别与主体策略

### 7.4.1 原则

由于用户开始打球后无法实时查看画面，网球模式不应依赖拍摄中手动点选主体。

系统应自动识别画面中的运动人物，并结合位置、运动幅度、持续时间判断主要拍摄对象。

---

### 7.4.2 主体判断策略

| 场景              | 策略               |
| --------------- | ---------------- |
| 画面中只有 1 人       | 自动作为主要拍摄对象       |
| 多人入画但只有 1 人明显运动 | 优先跟随运动更明显的人      |
| 双人对打            | 优先保持较大画面覆盖，不频繁切换 |
| 旁边球场有人经过        | 不立即切换主体          |
| 主体短暂离开画面        | 保持当前构图，等待主体返回    |
| 主体长时间丢失         | 重新识别主要运动人物       |

---

## 7.5 云台跟随策略

### 7.5.1 策略定位

网球模式不是「强跟人」，而是：

> 固定机位覆盖优先 + 轻量跟随修正。

---

### 7.5.2 画面区域策略

可将画面分为三个区域：

```text
[ 边缘危险区 ][ 跟随修正区 ][ 中央安全区 ][ 跟随修正区 ][ 边缘危险区 ]
```

| 人物位置  | 云台行为            |
| ----- | --------------- |
| 中央安全区 | 云台尽量不动，保持画面稳定   |
| 跟随修正区 | 云台缓慢修正，避免人物继续靠边 |
| 边缘危险区 | 云台提高响应，优先减少出画   |

---

### 7.5.3 运动场景策略

| 场景      | 策略                   |
| ------- | -------------------- |
| 小范围步伐调整 | 不频繁跟随                |
| 横向跑动    | 轻量跟随修正               |
| 快速冲刺    | 优先防止出画               |
| 急停反向    | 避免云台过度甩动             |
| 发球动作    | 保留上方空间，不因抛球短暂上移而大幅跟随 |
| 多人交叉    | 优先保持稳定，不频繁切主体        |

---

# 8. 重点片段保存方案

## 8.1 问题

一场网球训练或对打通常时间较长，如果全程保存大几十分钟高清视频，会带来几个问题：

| 问题      | 说明                   |
| ------- | -------------------- |
| 存储压力大   | 手机或相机空间消耗高           |
| 后期筛选麻烦  | 用户很难从几十分钟素材里找精彩回合    |
| 分享成本高   | 长视频不可直接发社媒           |
| 有效信息密度低 | 大量捡球、休息、等待、走位属于低价值内容 |

因此，网球模式需要支持：

> 长时间拍摄，但优先高清保存重点片段，非重点时段降级或临时缓存。

---

## 8.2 重点片段定义

本期可以先将以下片段识别为「重点片段」：

| 片段类型        | 判断依据              | 保存优先级 |
| ----------- | ----------------- | ----- |
| 多拍回合        | 连续多次击球/持续运动       | 高     |
| 长跑动回合       | 人物出现明显横向/前后跑动     | 高     |
| 快速冲刺/救球     | 运动速度明显提升          | 高     |
| 明显挥拍动作段     | 出现较大幅度击球动作        | 中     |
| 发球段         | 出现发球准备和挥拍动作       | 中     |
| 用户手动标记段     | 用户通过设备/手势/语音/按钮触发 | 高     |
| 长时间静止/捡球/休息 | 运动强度低             | 低     |

---

## 8.3 保存策略

### 8.3.1 推荐策略：循环缓存 + 重点片段高清落盘

拍摄过程中，系统持续进行低感知缓存，不立即把所有内容都作为正式素材保存。

```text
开始拍摄
↓
持续缓存最近一段时间画面
↓
识别到重点片段
↓
向前回溯保存片段前几秒
↓
持续保存至重点片段结束后几秒
↓
生成一个独立高清片段
↓
非重点内容继续滚动缓存或降级保留
```

---

### 8.3.2 片段保存规则

| 内容类型        | 处理方式             |
| ----------- | ---------------- |
| 重点片段        | 高清保存             |
| 重点片段前 3-5 秒 | 回溯保存，避免错过起手动作    |
| 重点片段后 3-5 秒 | 延后保存，保证动作完整      |
| 普通低价值时段     | 不高清保存，仅临时缓存或低清保留 |
| 长时间静止/休息    | 可丢弃或仅保留时间索引      |
| 用户手动标记片段    | 强制高清保存           |

---

### 8.3.3 用户可理解的产品表达

不要把这个能力叫得太技术化，可以叫：

> 重点片段保存

或者：

> 自动保留精彩回合

页面说明：

```text
开启后，将优先保存多拍回合、长跑动等重点片段，减少无效素材占用。
```

---

## 8.4 保存模式设计

建议提供两个保存模式。

### 模式 A：重点片段模式，默认推荐

| 项目    | 策略              |
| ----- | --------------- |
| 适用用户  | 大多数用户           |
| 保存内容  | 自动保存重点片段        |
| 非重点内容 | 临时缓存/不完整保存      |
| 优点    | 节省空间，后期筛选轻      |
| 缺点    | 可能漏掉部分用户认为重要的内容 |

文案：

```text
重点片段模式
自动保存多拍回合和长跑动片段，减少无效素材占用
```

---

### 模式 B：完整录制模式

| 项目    | 策略            |
| ----- | ------------- |
| 适用用户  | 希望完整复盘的用户     |
| 保存内容  | 全程录制          |
| 非重点内容 | 全部保存          |
| 优点    | 不漏内容          |
| 缺点    | 占用空间大，后期筛选成本高 |

文案：

```text
完整录制模式
保存完整视频，适合训练复盘，占用空间较大
```

---

### 默认建议

默认使用：

> 重点片段模式

理由：

* 符合用户“长时间打球不想存几十分钟垃圾素材”的需求；
* 更适合社媒分享和轻量复盘；
* 能突出云台智能能力；
* 降低存储压力。

完整录制作为用户可切换选项。

---

## 8.5 重点片段识别逻辑

本期不建议做过于复杂的网球语义识别，可以先基于运动强度和画面变化判断。

### 可用判断信号

| 信号       | 用途       |
| -------- | -------- |
| 人物移动速度   | 判断跑动、冲刺  |
| 人物移动距离   | 判断长跑动回合  |
| 挥拍动作幅度   | 判断击球动作   |
| 连续运动持续时间 | 判断回合是否持续 |
| 画面中人物活跃度 | 区分打球/休息  |
| 音频击球声    | 可辅助判断击球点 |
| 云台跟随变化   | 辅助判断主体运动 |
| 用户手动标记   | 作为强信号    |

---

### 第一版可以用的简化判断

重点片段满足任一条件即可保存：

| 条件         | 说明             |
| ---------- | -------------- |
| 连续运动超过一定时长 | 如连续明显运动 ≥ 8 秒  |
| 横向移动距离较大   | 如人物从画面一侧移动到另一侧 |
| 短时间速度变化明显  | 如冲刺、急停、救球      |
| 检测到连续挥拍动作  | 多次击球动作         |
| 用户主动标记     | 无论系统判断如何，直接保存  |

---

## 8.6 用户手动标记补充

虽然用户打球时不能频繁操作手机，但可以保留低成本标记方式。

可选方式：

| 标记方式    | 说明                | 优先级 |
| ------- | ----------------- | --- |
| 云台/设备按键 | 用户靠近设备时按一下，标记刚才片段 | P1  |
| 手机端按钮   | 用户中场休息时标记         | P1  |
| 手势标记    | 远距离/夜晚不稳定，不作为核心   | P2  |
| 语音标记    | 可探索，但球场噪声影响较大     | P2  |

本期建议：

> 先支持手机端「标记刚才片段」，如硬件条件允许，再支持设备按键标记。

标记逻辑：

```text
用户点击“标记”
↓
系统保存当前时间点前 15 秒 + 后 15 秒
↓
生成一个重点片段
```

文案：

```text
已标记刚才片段
```

---

## 8.7 非重点素材处理

非重点素材可以分三种处理策略，需结合技术实现能力选择。

### 方案 A：直接丢弃

| 项目 | 说明         |
| -- | ---------- |
| 方式 | 非重点时段不保存   |
| 优点 | 最省空间       |
| 缺点 | 用户无法找回漏判片段 |
| 适用 | 算法识别较准时    |

不建议第一版直接使用，风险较高。

---

### 方案 B：低清缓存

| 项目 | 说明                 |
| -- | ------------------ |
| 方式 | 非重点时段以低清/低码率形式临时保留 |
| 优点 | 节省空间，也有回看兜底        |
| 缺点 | 实现复杂度更高            |
| 适用 | 希望兼顾存储和找回能力        |

推荐作为理想方案。

---

### 方案 C：分段临时缓存，结束后用户选择

| 项目 | 说明                                  |
| -- | ----------------------------------- |
| 方式 | 拍摄过程中先分段缓存，结束后默认展示重点片段，完整素材可选择保留或删除 |
| 优点 | 用户有掌控感                              |
| 缺点 | 拍摄期间仍需要一定存储                         |
| 适用 | 第一版更稳妥                              |

本期建议优先：

> 重点片段高清保存 + 非重点分段临时缓存 + 拍摄结束后让用户选择是否保留完整素材。

---

## 8.8 拍摄结束页

拍摄结束后，页面不应该只告诉用户“保存成功”，而应该直接整理结果。

建议展示：

| 内容        | 说明               |
| --------- | ---------------- |
| 本次拍摄时长    | 如 48 分钟          |
| 已保存重点片段数量 | 如 12 段           |
| 重点片段总时长   | 如 4 分 30 秒       |
| 预计节省空间    | 可选               |
| 完整素材处理    | 保留完整素材 / 仅保留重点片段 |
| 快速查看      | 进入重点片段列表         |
| 快速剪辑      | 后续可接模板剪辑         |

示例：

```text
本次拍摄 48 分钟
已自动保存 12 个重点片段，共 4 分 30 秒

你可以仅保留重点片段，减少存储占用；也可以保留完整视频用于复盘。
```

操作按钮：

```text
查看重点片段
仅保留重点片段
保留完整视频
```

---

# 9. 页面流程

## 9.1 首次进入网球模式

```text
进入网球模式
↓
展示简短说明
↓
展示架机位引导
↓
用户调整设备
↓
选择保存模式
↓
开始拍摄
```

### 首次说明

```text
网球模式会根据球场画面和球员运动，自动修正取景并保存重点回合。
```

---

## 9.2 非首次进入网球模式

```text
进入网球模式
↓
展示简版架机位提示
↓
用户选择/沿用上次保存模式
↓
开始拍摄
```

---

## 9.3 拍摄中流程

```text
开始拍摄
↓
系统持续识别人物和运动状态
↓
进入低感知缓存
↓
识别到重点片段
↓
高清保存片段
↓
继续拍摄
↓
用户结束拍摄
```

---

## 9.4 拍摄结束流程

```text
结束拍摄
↓
展示重点片段统计
↓
用户查看重点片段
↓
用户选择完整素材处理方式
↓
保存结果进入相册
```

---

# 10. 页面与文案

## 10.1 拍摄前引导页

### 标题

```text
网球模式
```

### 说明

```text
自动修正取景，并优先保存多拍回合、长跑动等重点片段。
```

### 引导项

```text
1. 建议将设备架设在腰部至胸口高度
2. 建议横屏拍摄
3. 请让画面覆盖球网、底线和主要跑动区域
```

### 主按钮

```text
开始拍摄
```

### 次入口

```text
查看架机位示意
```

---

## 10.2 取景辅助提示

| 场景       | 文案                 |
| -------- | ------------------ |
| 竖屏拍摄     | 建议横屏拍摄网球运动         |
| 画面过近     | 建议适当拉远设备，保留跑动和挥拍空间 |
| 画面上方空间不足 | 建议上调画面，避免发球和挥拍出画   |
| 画面左右空间不足 | 建议覆盖左右边线，减少横向跑动出画  |
| 未对准球场    | 请将镜头对准网球场主要活动区域    |
| 光线较暗     | 当前光线较暗，可能影响识别和保存效果 |

---

## 10.3 保存模式选择

### 重点片段模式

```text
重点片段模式
自动保存多拍回合和长跑动片段，减少无效素材占用
```

### 完整录制模式

```text
完整录制模式
保存完整视频，适合训练复盘，占用空间较大
```

---

## 10.4 拍摄中提示

| 状态      | 文案                  |
| ------- | ------------------- |
| 正在拍摄    | 网球模式拍摄中             |
| 已识别重点片段 | 已保存重点片段             |
| 存储不足    | 存储空间不足，可能无法继续保存重点片段 |
| 识别效果较差  | 当前画面识别不稳定，建议调整机位或光线 |
| 用户标记    | 已标记刚才片段             |

---

## 10.5 拍摄结束页

```text
本次拍摄 48 分钟
已自动保存 12 个重点片段，共 4 分 30 秒
```

按钮：

```text
查看重点片段
仅保留重点片段
保留完整视频
```

---

# 11. 业务规则

## 11.1 拍摄模式规则

| 规则                     | 说明 |
| ---------------------- | -- |
| 用户进入网球模式后，默认启用广构图和轻量跟随 |    |
| 网球模式不默认启用动态长焦追踪        |    |
| 网球模式不依赖手势控制启动跟踪        |    |
| 用户可切换保存模式              |    |
| 用户结束拍摄后，需要明确处理完整素材     |    |

---

## 11.2 架机位规则

| 规则                   | 说明 |
| -------------------- | -- |
| 拍摄前优先展示场地标尺式引导       |    |
| 不要求用户站入画面完成校准        |    |
| 引导重点为球网、底线、边线、主要跑动区域 |    |
| 推荐设备高度为腰部至胸口         |    |
| 推荐横屏拍摄               |    |

---

## 11.3 重点片段规则

| 规则                 | 说明 |
| ------------------ | -- |
| 重点片段需包含片段前后缓冲      |    |
| 用户手动标记片段优先保存       |    |
| 系统识别的重点片段自动高清保存    |    |
| 非重点素材不默认作为最终高清素材保存 |    |
| 拍摄结束后用户可选择是否保留完整视频 |    |
| 存储不足时优先保留已识别重点片段   |    |

---

   `

一句话总结：

> 网球模式不是让用户拍摄中不断操作，也不是让云台强行追着人跑，而是让用户在场上没有自己的情况下也能先把机位架对，开始打球后由设备自动守住画面，并优先保存真正值得看的回合。
